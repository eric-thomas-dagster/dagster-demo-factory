{
  "component_type": "dagster_community_components.AzureDataFactoryComponent",
  "name": "Azure Data Factory Workspace",
  "description": "Azure Data Factory workspace component — one Dagster asset per ADF pipeline, trigger, and (untested) linked_service / dataset / data_flow / integration_runtime. Canonical `workspace:` block (AzureDataFactoryResource), `translation:` callable per-asset customization hook, `@public get_asset_spec(props)` subclass override, `AzureDataFactoryObjectProps` + `AzureDataFactoryComponentTranslator`. StateBackedComponent — discovery cached to disk; auto-refreshes on first `dg dev` OR run `dg utils refresh-defs-state` explicitly.",
  "category": "integration",
  "icon": "si:microsoftazure",
  "x-dagster-io": {
    "inputs":  {"type": "none", "description": "Discovery hits ADF Management API; no upstream Dagster inputs."},
    "outputs": {"type": "asset,sensor", "description": "One @multi_asset per ADF pipeline, external assets per trigger / linked_service / dataset / data_flow / integration_runtime, plus optional polling sensor for pipeline-run observation."}
  },
  "attributes": {
    "workspace":                     {"type": "object",  "required": true, "label": "Workspace",
                                      "description": "Azure Data Factory connection as an AzureDataFactoryResource: `{subscription_id, resource_group_name, factory_name, tenant_id_env_var?, client_id_env_var?, client_secret_env_var?}`. Falls back to DefaultAzureCredential when SP env vars aren't set. Secrets typically arrive via `{{ env.XXX }}` Jinja templating."},
    "translation":                   {"type": "string",  "label": "Translation",
                                      "description": "Python callable path resolved via Dagster's TranslationFn — customizes per-asset AssetSpec (key rename, extra tags/metadata, group override). Signature: `def fn(props: AzureDataFactoryObjectProps) -> AssetSpec`."},

    "import_pipelines":              {"type": "boolean", "label": "Import Pipelines", "default": true, "description": "Import ADF pipelines as materializable multi_assets (default true)."},
    "import_triggers":               {"type": "boolean", "label": "Import Triggers", "default": false, "description": "Import ADF triggers as observable external assets."},
    "import_linked_services":        {"type": "boolean", "label": "Import Linked Services (untested)", "default": false, "description": "**Untested.** Import ADF linked services (data source / sink connections) as external Dagster assets. Read-only surface — no runtime action. Validate against your factory before use."},
    "import_datasets":               {"type": "boolean", "label": "Import Datasets (untested)", "default": false, "description": "**Untested.** Import ADF datasets (schemas over linked services) as external Dagster assets. Read-only surface. Validate before use."},
    "import_data_flows":             {"type": "boolean", "label": "Import Data Flows (untested)", "default": false, "description": "**Untested.** Import ADF Mapping Data Flows (visual transformations) as external Dagster assets. Read-only surface. Validate before use."},
    "import_integration_runtimes":   {"type": "boolean", "label": "Import Integration Runtimes (untested)", "default": false, "description": "**Untested.** Import ADF Integration Runtimes (SSIS / Azure IR / Self-hosted IR) as external Dagster assets. Read-only surface. Validate before use."},

    "filter_by_name_pattern":        {"type": "string",  "label": "Filter By Name Pattern", "description": "Regex — only entities whose names match are imported. Applied to every kind."},
    "exclude_name_pattern":          {"type": "string",  "label": "Exclude Name Pattern", "description": "Regex — entities whose names match are excluded. Applied after `filter_by_name_pattern`."},
    "filter_by_tags":                {"type": "string",  "label": "Filter By Tags", "description": "Comma-separated tag keys that entities must carry."},

    "polling_sensor":                {"type": "boolean", "label": "Polling Sensor", "default": true, "description": "Emit a polling sensor that observes ADF pipeline-run status and emits AssetObservation events."},
    "poll_interval_seconds":         {"type": "integer", "label": "Poll Interval Seconds", "default": 60},

    "group_name":                    {"type": "string",  "label": "Group Name", "default": "azure_data_factory"},
    "description":                   {"type": "string",  "label": "Description"},
    "owners":                        {"type": "array",   "label": "Owners", "items": {"type": "string"}},
    "asset_tags":                    {"type": "object",  "label": "Asset Tags"},
    "extra_kinds":                   {"type": "array",   "label": "Extra Kinds", "items": {"type": "string"}, "description": "Additional `dagster/kind/*` tags applied to every asset."},
    "asset_key_prefix":              {"type": "array",   "label": "Asset Key Prefix", "items": {"type": "string"}, "description": "Optional key prefix — every asset key gets `[<prefix>..., adf_<kind>_<name>]`."},

    "assets_by_pipeline_name":       {"type": "object",  "label": "Assets By Pipeline Name", "description": "Legacy hook: override or expand AssetSpecs for specific ADF pipelines. Keys are ADF pipeline names; values are single spec-override dicts or lists. For finer-grained per-asset customization, prefer the `translation:` callable."},

    "pipeline_parameters":           {"type": "object",  "label": "Pipeline Parameters", "description": "Parameters dict passed to every ADF pipeline run (key → value)."},
    "partition_parameter_name":      {"type": "string",  "label": "Partition Parameter Name", "description": "When the asset is partitioned, auto-pass the partition_key as this ADF pipeline parameter."},
    "max_wait_seconds":              {"type": "integer", "label": "Max Wait Seconds", "default": 3600},
    "run_poll_interval_seconds":     {"type": "integer", "label": "Run Poll Interval Seconds", "default": 30},
    "wait_for_completion":           {"type": "boolean", "label": "Wait For Completion", "default": true},
    "capture_activity_metadata":     {"type": "boolean", "label": "Capture Activity Metadata", "default": true},

    "partition_type":                {"type": "string",  "label": "Partition Type", "enum": ["daily", "weekly", "monthly", "hourly", "static"]},
    "partition_start":               {"type": "string",  "label": "Partition Start"},
    "partition_values":              {"type": "array",   "label": "Partition Values", "items": {"type": "string"}},

    "freshness_max_lag_minutes":     {"type": "integer", "label": "Freshness Max Lag Minutes"},
    "freshness_cron":                {"type": "string",  "label": "Freshness Cron"},
    "upstream_asset_keys":           {"type": "array",   "label": "Upstream Asset Keys", "items": {"type": "string"}},
    "retry_policy_max_retries":      {"type": "integer", "label": "Retry Policy Max Retries"},
    "retry_policy_delay_seconds":    {"type": "integer", "label": "Retry Policy Delay Seconds"},
    "retry_policy_backoff":          {"type": "string",  "label": "Retry Policy Backoff", "default": "exponential"},

    "defs_state":                    {"type": "object",  "label": "Defs State", "description": "State backend for cached workspace discovery. Local filesystem by default; override per-deploy for Dagster Cloud."}
  },
  "tags": [
    "integration",
    "azure",
    "adf",
    "data-factory",
    "workspace",
    "state-backed"
  ]
}
