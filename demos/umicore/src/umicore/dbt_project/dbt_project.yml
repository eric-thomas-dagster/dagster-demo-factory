name: "umicore"
version: "1.0.0"
config-version: 2

profile: "umicore"

model-paths: ["models"]
clean-targets: ["target", "dbt_packages"]

models:
  umicore:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
