type: northwind_logistics.components.CarrierRateFeedComponent

attributes:
  demo_mode: true
  demo_seed: 20260824
  warehouse:
    demo_mode: true
    user: "{{ env('SNOWFLAKE_USER', '') }}"
    account: "{{ env('SNOWFLAKE_ACCOUNT', '') }}"
    password: "{{ env('SNOWFLAKE_PASSWORD', '') }}"
    database: "{{ env('SNOWFLAKE_DATABASE', 'NORTHWIND') }}"
    schema: "{{ env('SNOWFLAKE_SCHEMA', 'RAW') }}"
    warehouse: "{{ env('SNOWFLAKE_WAREHOUSE', '') }}"
