{
  "component_type": "dagster_component_templates.MSSQLIOManagerComponent",
  "description": "Persist DataFrames to a Microsoft SQL Server schema, one table per asset.",
  "category": "io_manager",
  "icon": "Save",
  "x-dagster-io": {
    "outputs": {
      "type": "IOManager",
      "description": "Resource"
    }
  },
  "attributes": {
    "resource_key": {
      "type": "string",
      "label": "Resource Key",
      "default": "io_manager",
      "required": false
    },
    "connection_url_env_var": {
      "type": "string",
      "label": "Connection Url Env Var",
      "required": true
    },
    "schema_name": {
      "type": "string",
      "label": "Schema Name",
      "required": true
    },
    "if_exists": {
      "type": "string",
      "label": "If Exists",
      "required": true,
      "enum": [
        "fail",
        "replace",
        "append"
      ],
      "ui:widget": "select"
    }
  },
  "tags": [
    "io_manager",
    "mssql",
    "sqlserver",
    "sql"
  ],
  "name": "MSSQLIOManager"
}
