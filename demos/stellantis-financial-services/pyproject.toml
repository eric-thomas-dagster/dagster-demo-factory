[project]
name = "stellantis_financial_services"
requires-python = ">=3.10,<3.15"
version = "0.1.0"
dependencies = [
    "dagster==1.13.19",
    "dagster-cloud>=1.13.19",
    "pandas>=2.0",
    "numpy>=1.26",
    "duckdb>=1.0",
    "azure-identity>=1.15",
    "requests>=2.31",
]

[dependency-groups]
dev = [
    "dagster-dg-cli",
    "dagster-webserver",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
force-include = { "pyproject.toml" = "pyproject.toml" }

[tool.dg]
directory_type = "project"

[tool.dg.project]
root_module = "stellantis_financial_services"
registry_modules = [
    "stellantis_financial_services.components.*",
]



[project.entry-points."dagster_dg_cli.registry_modules"]
stellantis_financial_services_components = "stellantis_financial_services.components"
