[project]
name = "detroit_dwsd"
requires-python = ">=3.10,<3.15"
version = "0.1.0"
dependencies = [
    "dagster==1.13.20",
    "dagster-cloud>=1.13.20",
]

[dependency-groups]
dev = [
    "dagster-dg-cli",
    "dagster-webserver",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
force-include = { "pyproject.toml" = "pyproject.toml" }

[tool.dg]
directory_type = "project"

[tool.dg.project]
root_module = "detroit_dwsd"
registry_modules = [
    "detroit_dwsd.components.*",
]



[project.entry-points."dagster_dg_cli.registry_modules"]
detroit_dwsd_components = "detroit_dwsd.components"
