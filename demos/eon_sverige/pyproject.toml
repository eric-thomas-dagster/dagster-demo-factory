[project]
name = "eon_sverige"
requires-python = ">=3.10,<3.15"
version = "0.1.0"
dependencies = [
    "dagster==1.13.21",
    "dagster-cloud>=1.13.21",
]

[dependency-groups]
dev = [
    "dagster-dg-cli",
    "dagster-webserver",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
force-include = { "pyproject.toml" = "pyproject.toml" }

[tool.dg]
directory_type = "project"

[tool.dg.project]
root_module = "eon_sverige"
registry_modules = [
    "eon_sverige.components.*",
]



[project.entry-points."dagster_dg_cli.registry_modules"]
eon_sverige_components = "eon_sverige.components"
